extends ../layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/product.css")


block content
  .text-center
    h1 #{product.title}
    hr
    div
      img(src=(product.imageUrl), alt=(product.title), width="640", height="480")
    h2 ₹#{product.price}
    p #{product.description}
    include ../includes/add-to-cart.pug
